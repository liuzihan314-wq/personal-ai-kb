# Personal AI Knowledge Base — PRODUCT.md

> Version: v0.2
> Status: V1 baseline approved；V2 multi-user design proposal（未实现）
> V1 primary user: Myself
> V1 primary runtime: macOS
> V2 planned runtime: Tencent Cloud CVM + Cloudflare Access
> Compatibility target: Windows backup runtime

## 1. 一句话定义

一个面向我个人使用的 AI 内容知识库：收集和整理我平时学习 AI 的优质资料，把零散内容沉淀成可持续调用的个人知识，并进一步辅助我生成 AI 提效方向的自媒体选题和 2～3 分钟口播文案。

核心价值不是“收藏更多”，而是：

**让以前看过的资料在未来真正能够被重新调用，并转化为我的内容输出。**

---

## 2. 用户与核心问题

V1 只有一个用户：我自己。

当前主要问题：

- AI 学习资料分散在微信公众号收藏、知识星球 PDF 等位置。
- 收藏之后，过一段时间很难快速回忆、定位和重新利用。
- 写 AI 内容时，经常又从头搜索、阅读、整理。
- 单篇摘要不够，我希望把多篇资料逐渐编译成对某个主题的“当前认知”。
- 最终希望知识库参与选题和口播创作，而不是停留在资料管理。

### 2.1 V1 与 V2 边界

V1 是当前稳定基线：单用户、本地运行、默认使用本机 `data/` 目录，不提供登录、角色、团队协作或云端部署。V1 的 PDF、观点卡片、检索、问答、主题综合、选题和口播闭环必须继续可回归。

V2 是在 V1 核心业务之上增加的受邀多用户 Web 访问方案，首个最小切片只验证两个测试身份：管理员 A 和普通成员 B。V2 通过 Cloudflare Access 提供 HTTPS 与登录入口，腾讯云服务器运行现有 Python／Streamlit 应用；应用根据已验证身份选择用户数据根目录。

V2 首期不引入公开注册、匿名访问、跨用户共享、管理员默认读取成员内容、计费、移动 App 或新的知识来源。V2 未完成前，文档中的云端、认证、角色和隔离能力都属于规划，不代表当前代码已经支持。

---

## 3. V1 输入

### 3.1 微信公众号文章

V1 必须提供可靠的 **Manual WeChat Article Import**：用户主动提供微信公众号文章 URL；自动提取失败时，可以补充标题、正文和原始 URL 完成导入。

微信收藏自动增量同步保留为 Experimental / POC，不是 V1 主闭环的阻塞条件。

两种入口都采用白名单逻辑，只允许确认属于微信公众号文章的内容进入知识库。

明确排除：

- 收藏的视频
- 视频号
- 普通网页 URL / 普通链接
- 图片
- 音频
- 小程序
- 聊天记录
- 文件类收藏
- 其他非微信公众号文章内容

普通网页 URL 不会因为由用户手动输入而自动成为公众号知识来源。

### 3.2 文本 PDF

主要用于知识星球下载内容和其他正常文本 PDF。

V1 仅支持：

- 本身有正常文本层
- 可以正常复制文字的 PDF

V1 明确不支持：

- 扫描 PDF
- 图片型 PDF
- OCR

### 3.3 观点 / 好句 / 灵感卡片

允许用户手动保存：

- 自己的观点
- 好句
- 灵感
- 判断
- 值得以后引用的表达

最少字段：

- 正文
- 来源（可选）
- 时间
- 主题 / 标签（可选）
- 个人备注（可选）

这些卡片应参与搜索、主题综合、选题和口播生成。

---

## 4. V2 预留输入

V1 不实现，但架构必须为以下来源留扩展口：

- 小红书文章 / 内容
- YouTube 视频链接 → 字幕 / 文本
- 历史口播稿与最终修改稿，用于后续表达风格学习

---

## 5. 单篇 AI 加工

每份完整资料进入知识库后，AI 至少生成：

- 摘要
- 核心观点
- 值得保留 / 引用的表达
- 标签 / 主题信息
- 来源追踪信息

AI 生成内容不得覆盖原始资料。

---

## 6. 多资料编译

系统不能停留在“一篇资料对应一篇摘要”。

当多篇资料讨论同一主题时，应能够逐渐编译成主题级知识页，例如：

`AI视频生成.md`

内容可包括：

- 主要路线
- 共识
- 分歧
- 原理
- 优缺点
- 案例
- 当前综合判断
- 对应来源

目标是形成：

**关于这个主题，我目前一共知道什么。**

---

## 7. V1 核心输出

### 7.1 知识问答

例如：

- 知识库里关于 AI 生成视频有哪些思路？
- 我之前收集过哪些 Agentic Coding 的观点？
- 有哪些资料讨论 AI 如何提升办公效率？

回答应优先依据个人知识库，而不是完全自由发挥。

### 7.2 主题综合

把多份相关资料整理为：

- 主要路线
- 共识
- 差异观点
- 优缺点
- 案例
- 当前综合结论

### 7.3 AI 自媒体选题

根据整个知识库生成 3～5 个候选选题。

要求：

- 候选范围是整个知识库
- 近期新增内容适当提高权重
- 主题整体围绕 AI 提效 / AI 工作效率 / AI 工具 / AI 工作流 / Agent / AI 内容创作 / AI 视频 / AI 职场应用
- 识别信息差、趋势、冲突观点、案例和普通用户痛点

### 7.4 口播生成

流程必须是：

候选选题  
→ 用户人工选择  
→ 系统重新检索相关知识和来源  
→ 组织写作上下文  
→ 生成约 2～3 分钟口播稿

要求：

- 自然口语
- 有明确观点
- 有实际内容支撑
- 避免纯鸡汤
- 尽量利用个人知识库已有资料，而不是凭空生成

---

## 8. 数据原则

### 8.1 Raw 永远是事实来源

原始资料入库后，AI 不得覆盖和修改。

AI 的摘要、观点、标签、主题综合必须与 Raw 分开。

### 8.2 AI 内容必须可追溯

尽可能形成：

Knowledge  
→ Notes  
→ Raw

的追溯链。

### 8.3 个人观点也是正式知识

系统不能退化成“自动收藏文章的软件”。

---

## 9. 使用方式

### Web UI

日常主要入口，用于：

- 导入 PDF
- 添加观点卡片
- 浏览资料
- 搜索 / 问答
- 主题综合
- 生成选题
- 选择选题
- 生成口播

### CLI

主要用于：

- 自动任务
- 开发调试
- Codex 验收
- 定时同步
- 故障排查

Web UI 与 CLI 必须共用核心业务逻辑。

---

## 10. 平台要求

### macOS

- 当前主要开发环境
- 当前主要运行环境
- 微信收藏自动同步 POC 已验证为 `BLOCKED`，后续仅在重新批准的独立 POC 中继续
- Manual WeChat Article Import 是 V1 稳定入口
- 使用 macOS 原生调度能力完成定时任务

### Windows

- 保留为备用运行环境
- 核心业务代码尽量跨平台
- 不建立长期 `mac` / `windows` 两套代码分支
- 平台差异通过 Adapter 隔离
- Windows 微信同步可作为后续独立 POC，不阻塞 V1 主线，也不得描述为已完成方案

### V2 腾讯云运行环境

- Cloudflare Access 保护公开 Web 入口，并负责登录策略；无身份请求在到达应用前被拒绝。
- Cloudflare Tunnel 将入口转发到腾讯云 CVM 上的本机 Streamlit 服务；应用端口不直接作为公开入口。
- 腾讯云只负责运行现有 Python／Streamlit 应用和持久化用户数据，不改变 V1 的 Raw、Notes、Knowledge、Index 业务语义。
- V2 的实例规格、地域、域名、身份提供商和备份策略尚未确定，不能写入真实凭据或伪装成已部署。

---

## 11. V1 明确不做

- 多用户
- 登录 / 权限
- 团队协作
- 商业化
- 移动 App
- 扫描 PDF / OCR
- 微信收藏视频
- 视频号内容
- 微信收藏普通 URL
- 小红书自动读取
- YouTube 自动读取
- 历史口播风格学习
- 自动生成视频
- 自动剪辑
- 自动配音
- 自动发布平台
- 重型 RAG / 向量数据库作为 V1 必选项
- 云服务器部署

以上是 V1 的边界，不是对 V2 的完成声明。V2 只在独立任务通过验收后逐步开启认证、角色、用户目录和历史隔离。

---

## 12. V1 成功标准

至少准备 5 篇 AI 相关资料，跑通：

1. 导入资料，其中微信公众号文章可通过 Manual WeChat Article Import 可靠进入系统
2. 原始内容正确保存
3. AI 生成摘要、核心观点和好句
4. 可以搜索 / 问答
5. 可以综合多篇资料
6. 可以生成 3～5 个 AI 提效选题
7. 用户人工选择其中一个
8. 系统重新调用相关知识
9. 生成一份 2～3 分钟口播稿

核心价值判断：

> 当我下一次准备做 AI 口播时，是否不需要重新从零搜索资料？

若答案为“是”，MVP 开始产生真实价值。

V1 成功不依赖微信收藏自动同步通过。

---

## 13. 微信输入独立验收

### 13.1 Manual WeChat Article Import（V1 MUST HAVE）

至少验证：

1. 接受明确的微信公众号文章 URL
2. 拒绝普通网页 URL、视频号及其他不受支持的微信链接
3. 自动提取成功时生成现有 Unified Document
4. 自动提取失败时不写入残缺 Raw，并允许用户提交标题、正文和原始 URL
5. Raw 保持 immutable
6. 同一文章重复导入不重复写入
7. 导入失败不影响其他 Input Adapter
8. 使用一篇真实公众号文章完成最小端到端验收

### 13.2 Favorites Auto Sync（Experimental / POC）

至少验证：

1. 微信收藏新增一篇公众号文章
2. 系统发现新增
3. 正确识别为公众号文章
4. 正确保存正文和来源信息
5. 第二次同步不重复导入
6. 视频收藏不进入知识库
7. 普通 URL 不进入知识库
8. 其他非公众号收藏不进入知识库
9. 自动同步失败时仍可使用 Manual WeChat Article Import
10. 微信同步失败不能影响 PDF、观点卡片、检索、选题和口播主链路

## 14. V2 多用户访问（规划，未实现）

### 14.1 身份与角色

Cloudflare Access 的自托管应用策略只允许受邀身份进入 Web 入口。应用不把浏览器提交的邮箱、用户目录或角色当作可信输入，而是读取并校验 Access 应用 JWT，从可信的 `iss`、`aud`、`exp` 和 `sub` 得到当前身份。邮箱只用于页面展示和必要的审计信息，不作为用户目录的唯一键。

首期角色由服务器侧配置把已校验的 `sub` 映射为 `admin` 或 `member`，一个身份只能命中一个角色；未知或冲突映射直接拒绝。Cloudflare 的“允许进入”与应用的“能做什么”是两层边界，不能只配置前者就认为完成了角色权限。

| 角色 | 首期允许 | 首期不允许 |
| --- | --- | --- |
| `admin` | 登录、查看自己的知识库、执行服务健康检查；身份／角色配置仍通过受控运维流程维护 | 默认读取、检索、下载或查看 `member` 的 Raw、Notes、Knowledge、Index 和历史 |
| `member` | 登录、导入资料、检索自己的知识库、生成自己的问答／选题／口播并查看自己的历史 | 读取其他用户目录、修改角色配置、查看全局历史或绕过应用读取服务器文件 |

管理员是运行维护角色，不是天然的数据共享角色。若未来需要管理员审计成员内容，必须另立明确的只读审计范围、授权记录和验收，不属于 V2 首期。

### 14.2 用户数据与历史隔离

V2 为每个已认证身份建立独立根目录：

```text
data/users/<user_id>/
├── raw/
├── notes/
├── knowledge/
├── index/
└── history/
```

`<user_id>` 由应用根据 `iss` 与 `sub` 稳定生成，不能由请求参数、上传文件名或邮箱直接指定。Raw、Notes、Knowledge、Index、上传文件、导出文件、缓存和问答／选题／口播历史都必须绑定当前用户根目录；列表、检索、来源展示和下载也必须先限定在这个根目录内。

V1 继续使用现有本地 `data/raw/`、`data/notes/`、`data/knowledge/` 和 `data/index/` 目录。V2 不在首次启动时静默迁移或覆盖 V1 数据；迁移必须是单独任务，先备份、生成清单、显式确认，再把数据复制到指定用户目录。回滚时保留 V1 原目录和 V2 用户目录，不把云端认证失败自动降级为共享单用户目录。

### 14.3 V2 最小验收与失败路径

V2 首期必须用身份 A／B 证明：两者能通过同一 Cloudflare Access 入口到达应用；页面显示当前身份和角色；各自写入独立目录；A 与 B 在列表、检索、问答来源、下载和历史中互不可见；无身份请求被拒绝；V1 本地模式的既有回归测试继续通过。

以下情况必须失败关闭，不得回退到共享目录：Access 拒绝、JWT 缺失或校验失败、`sub` 缺失、`iss`／`aud`／过期时间不符合配置、身份没有唯一角色、用户目录路径非法、目录创建或读写失败，以及请求试图访问其他用户路径。前端只显示可读错误，不显示认证头、密钥或服务器绝对路径。
